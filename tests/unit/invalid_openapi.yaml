openapi: 3.0.0
info:
  version: 1.0.0
  title:
paths:
  /jsonBody/empty:
    post:
    operationId: jsonEmptyBody
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                id:
                  type: string
      responses:
        default:
          description: ok